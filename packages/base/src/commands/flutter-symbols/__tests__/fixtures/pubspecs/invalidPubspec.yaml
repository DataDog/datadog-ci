name: invalid_pubspec_example
description: A pubspec that has invalid yaml (sdk line doesn't close it's string)
version: 1.2.3-test1


environment:
  sdk: ">=2.17.0-0 <3.0.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  datadog_flutter_plugin: ^1.0.0-beta.3
  uuid: ^3.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:

