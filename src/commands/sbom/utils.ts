export const getBaseIntakeUrl = () => {
  // if (process.env.DATADOG_SITE || process.env.DD_SITE) {
  //   return `https://sbom-intake.${process.env.DATADOG_SITE || process.env.DD_SITE}`
  // }

  // return 'https://sbom-intake.datadoghq.com'
  return 'https://sbom-intake.datad0g.com'
  // return 'https://webhook.site';
}
